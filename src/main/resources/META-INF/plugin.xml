<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.dockerfilemissingusercompanion</id>
    <name>Dockerfile Missing USER Instruction Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning when a Dockerfile's FINAL build stage never sets a
        non-root <code>USER</code> -- Docker runs a container as root by
        default when <code>USER</code> is absent, a real risk documented
        explicitly by Docker's own guide for the instruction (container
        escape/privilege escalation if a volume mount is
        misconfigured).</p>
        <p><b>Reasons over real multi-stage structure</b> -- a
        <code>USER</code> instruction in an EARLIER stage never counts;
        only the file's last <code>FROM</code> (the stage a plain
        <code>docker build</code> with no <code>--target</code> actually
        runs) is checked. Confirmed with the strongest evidence
        available: an open issue in hadolint's own repository
        (<code>hadolint/hadolint#1089</code>) asking for exactly this
        rule, unresolved -- not even the standard Dockerfile CLI linter
        has it implemented yet.</p>
        <p>Stated honestly: v0.1 only detects total absence of a
        non-root <code>USER</code> in the final stage -- never validates
        that the referenced user actually exists (<code>USER appuser</code>
        with no prior <code>RUN useradd appuser</code> is a different
        problem, out of scope here).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="Dockerfile Missing USER Instruction Companion"
            displayName="Final stage never switches to a non-root USER"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.dockerfilemissingusercompanion.inspection.MissingUserInspection"/>
        <notificationGroup id="Dockerfile Missing USER Instruction Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
